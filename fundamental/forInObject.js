let subjects = {
	computer : true,
	science: false,
	english: false
}

// Object.keys(subjects) ==> return arr of all keys

for(let key in subjects){
	console.log(`key`, key, `value`, subjects[key], subjects.key) ;
}
let subject = {name: "Shailja"}
// console.log(input);

//s0h5
for(let key in subject){
    console.log(subject.key);
}
